package org.cataloguemanager.models;

public class Student extends User {
    public Student(String name, String memberId, String email, String phoneNumber) {
        super(name, memberId, email, phoneNumber);
    }
}
